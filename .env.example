# Neon connection string — set this in Vercel's Project Settings > Environment
# Variables for production, and in a local .env (gitignored) for `vercel dev`.
# Copy the pooled connection string from the Neon dashboard; it includes
# sslmode=require, which server/db.js checks for to enable SSL.
DATABASE_URL=
