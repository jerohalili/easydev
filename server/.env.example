# Local Docker Postgres:
# DATABASE_URL=postgresql://postgres:postgrespassword@localhost:5432/easydev
#
# Neon (production): copy the pooled connection string from the Neon
# dashboard. It already includes sslmode=require, which db.js checks for
# to enable SSL.
DATABASE_URL=

PORT=5000
