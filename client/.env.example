# Base URL of the deployed API (Render), used only in production builds.
# Leave unset for local dev — App falls back to http://localhost:5000/api.
VITE_API_BASE=https://your-render-service.onrender.com/api
